namespace Ouroboros.Controls.Views;

/// <summary>Interaction logic for DashboardView.xaml — per-client live status cards (bound to the shell view-model).</summary>
public sealed partial class DashboardView
{
    public DashboardView() => InitializeComponent();
}
