<UserControl x:Class="Ouroboros.Controls.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModel="clr-namespace:Ouroboros.ViewModel"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModel:InventoryViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding Header}"
                   Margin="0,0,0,6"
                   Opacity="0.8" />

        <ListView Grid.Row="1"
                  ItemsSource="{Binding Items}"
                  FontFamily="Consolas">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Slot"
                                    DisplayMemberBinding="{Binding Slot}"
                                    Width="50" />
                    <GridViewColumn Header="Name"
                                    DisplayMemberBinding="{Binding Name}"
                                    Width="240" />
                    <GridViewColumn Header="Count"
                                    DisplayMemberBinding="{Binding Count}"
                                    Width="70" />
                    <GridViewColumn Header="Durability %"
                                    DisplayMemberBinding="{Binding DurabilityPercent}"
                                    Width="90" />
                    <GridViewColumn Header="Sprite"
                                    DisplayMemberBinding="{Binding Sprite}"
                                    Width="70" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
